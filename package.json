{
  "name": "fuel-sharing",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node --check utils.js && node --check supabase-helpers.js && node --check data-store.js && node --check settlement-calculations.js && node --check ui-messages.js && node --check audit-log.js && node --check notifications.js && node --check admin-tools.js && node --check app.js && node --check service-worker.js && node --check tools/check-app-references.mjs && python3 -m py_compile server.py && python3 -m json.tool ledger-data.json > /dev/null && node tools/check-app-references.mjs",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
