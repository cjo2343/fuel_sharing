# Rename runbook: GoVehlo → VehloShare (vehloshare.app)

Decision (2026-07-09): rebrand **GoVehlo → VehloShare**, primary domain **vehloshare.app**.
The **"Vehlo" brand root survives** — this is a `Go`-prefix → `Share`-suffix swap plus a
domain move, not a new identity. The Vehlo mark/icon barely changes; the churn is in the
wordmark and every `govehlo.*` identifier.

**Hard constraint:** land this **before the first TestFlight / App Store submission.**
Changing the app's bundle id after submission means a *new app* (lost reviews/history).
We are pre-launch (DEMO_MODE gated, GVM-117 TestFlight pass still open), so this is the
cheapest this will ever be. Every week of new infra on `govehlo.*` raises the cost.

Legend: **[YOU]** = operator/dashboard/DNS action · **[CLAUDE]** = code/config change I do.

---

## Progress (2026-08-06) — done vs remaining

The identity/infra sweep that this document called "one coordinated cutover, all of it
Apple-gated" turned out **not** to be one block: everything that does not need an Apple
Developer account has now shipped. What is left is the genuinely Apple-gated set plus two
cosmetic service renames and the final domain 301.

**Done — Phase 1 (domain) + admin URL cutover (2026-07-09):**
- `vehloshare.app` + `www` live on the Pages project; `vehloshare.com` 301 → `.app`. TLS valid.
- `admin.vehloshare.app` is the canonical, Cloudflare-Access-gated console; legacy
  `admin.govehlo.dk` 301s to it; the `/admin` bypass on every public origin is guarded
  (web **#82/#83**). Supabase magic-link redirect allowlisted for the new host.

**Done — Phase 5 display-string sweep (deliberate early flip, 2026-07-09):** shipped ahead of
cutover on purpose — the site has no traffic yet, so there's no cost to flipping public-facing
copy now. This was a deliberate call, not an accidental merge; the earlier "HOLD until cutover"
framing for this piece is obsolete. Web **#87** (merge `3bd8e7e`): landing title,
"VehloShare Admin" console title, `manifest.json`, all `email-templates/*.html`, `landing.js`,
the privatliv page. Mobile **#264** (merge `6364a07`): 14 files incl. the SignInScreen
wordmark and `expo.name` → "VehloShare", plus `485b14e` (iOS location-permission string).

**Done — Phase 3 app identity, mobile (2026-08-05, mobile #571):** bundle id
`ios.bundleIdentifier` **and** `android.package` → **`app.vehloshare`**; `expo.scheme` → the
**array** `["vehloshare", "govehlo"]`. Three things about that shape are deliberate and are
**not** unfinished work:
- `vehloshare` is canonical and the only scheme the app ever *emits* (`APP_SCHEME` in
  `src/lib/auth-linking.ts`); `govehlo` stays in the array so links sent before the rebuild
  still open the app. Drop it once nothing in the wild emits it.
- **`slug` deliberately stays `govehlo`.** EAS `appVersionSource: "remote"` reads the
  server-side project record; renaming the slug locally desynchronises it. The rename belongs
  on expo.dev first and is invisible to users either way.
- `ios/` is **not committed** (gitignored, regenerated by `expo prebuild`), so the old
  "propagate into the committed native projects" step has no target — see the struck item in
  the checklist below.
`applinks:vehloshare.app`, the `vehloshare.app` intent-filter host, the API base and the
privacy URL had already landed in GVM-436 and earlier.

**Done — web identity constants (2026-08-05, web #275):** AASA `BUNDLE_ID` and
assetlinks `PACKAGE_NAME` → `app.vehloshare`; `/join/[code].js` `APP_SCHEME` → `vehloshare`;
the announcement composer's `ALLOWED_LINK_SCHEMES` gained `vehloshare://` (kept `govehlo://`
through the transition). The association files themselves still need the **Apple Team ID** —
that half is Phase 4 and stays Apple-gated.

**Done — Supabase dashboard, owner actions (2026-08-06):**
- `vehloshare://auth/callback` added to the Supabase redirect allowlist (`govehlo://` kept
  through the transition, Decision 3).
- **Phase 2 step 3 closed:** Supabase Auth SMTP sender switched to `no-reply@vehloshare.app`,
  and the rebranded email templates were **re-pasted into Supabase** (the repo copies are not
  live by themselves).

**Done — mailboxes moved to Proton Mail (2026-08-05/06):** `vehloshare.app` inbound mail is
Proton (`MX mail.protonmail.ch` / `mailsec.protonmail.ch`, Proton SPF on the apex, three DKIM
CNAMEs). **`support@vehloshare.app` and `services@vehloshare.app` are real mailboxes.**
**Cloudflare Email Routing on `vehloshare.app` was retired in the same cutover** — the
checklist item that called for standing it up is obsolete, not outstanding. The Sweego rows are
untouched (`swg.vehloshare.app` return-path SPF + `sweego1._domainkey` DKIM), so transactional
and human mail coexist on the apex. DMARC on `vehloshare.app` has been
`p=quarantine; rua=mailto:services@vehloshare.app` since 2026-08-06; the remaining tail is the
`p=reject` flip after a clean burn-in. The authoritative record is
`docs/dmarc-records.md` in the web repo — do not restate it here.

**Done — GitHub repo renames (2026-08-06):** `govehlo-web` → **`vehloshare-web`**,
`govehlo-mobile` → **`vehloshare-mobile`**. `fuel_sharing` has no "govehlo" in its name and was
**not** renamed (optional, not taken). GitHub redirects the old names, local remotes were
updated, and **Cloudflare Pages auto-deploy was verified still firing after the rename**.
⚠️ **The local working directories are still `govehlo-web/` and `govehlo-mobile/`** — every
cross-repo tool resolves siblings by those paths. Repo name ≠ directory name from here on.

**Still open — the Apple-gated set (unchanged, GV-320):**
- Apple Developer Program enrolment; App ID **`app.vehloshare`** with **Associated Domains**;
  App Store name reservation for "VehloShare".
- **Phase 4 (universal links)** — needs the Apple **Team ID** before the AASA/assetlinks files
  can carry a real `appID`; the end-to-end link test has to run on **TestFlight**, not the
  simulator.

**Still open — everything else:**
- **Sentry org + Supabase project *display* names** still say "govehlo" (cosmetic; the Supabase
  ref/URL never changes).
- **Phase 6** — the `govehlo.dk` → `vehloshare.app` 301, which is deliberately **LAST**
  (308 for `/api/*`).
- Retiring `govehlo.dk`'s legacy sender, and hardening `govehlo.dk`'s DMARC once it sends
  nothing.

**Nothing is broken.** `govehlo.dk` keeps serving the identical Pages deployment, `govehlo://`
is still answered by the app, and old join/auth links still resolve. The app now builds against
`app.vehloshare` — an existing simulator/dev install under the old `dk.govehlo.app` must be
uninstalled once or you get two icons.

---

## Good news up front (things that DON'T have to change)

- **The Supabase database does not move.** Project ref stays `kdudfqzglhydmzntqosb`. No
  data migration — only the project *display name*, the auth redirect allowlist, and the
  SMTP sender address change.
- **Jira keys stay `GV` / `GVM`.** Renaming keys breaks every commit/PR/issue reference.
  Rename only the project *display names*. (Not worth the churn.)
- **The bundle id is invisible to users** — see Decision 1. You are not *forced* to change it.

---

## Domain portfolio (roles are fixed — don't blur them)

Exactly **one** canonical domain: `vehloshare.app`. Everything else is a 301 redirect to it.
Two "canonical" domains = a universal-link mismatch (the entitlement lists only
`vehloshare.app`) plus split-brain email/SEO.

| Domain | Role | Setup |
|---|---|---|
| **vehloshare.app** | **Canonical — the only source of truth.** App `associatedDomains`, universal-link files (`/.well-known/*`), auth/join links, and the Sweego email sending domain all live here. | Cloudflare zone → Pages custom domain; Sweego SPF/DKIM/DMARC; hosts the association files. |
| **vehloshare.com** | Redirect only (brand / type-in protection). | Cloudflare zone + a 301 → `vehloshare.app`. **No** email, **no** universal links, **not** a Pages app domain. |
| **vehloshare.dk** | Optional, redirect only (DK-first market). | Same as `.com`: 301 → `vehloshare.app`. |
| **govehlo.dk** | Legacy — redirect only after cutover (Decision 3). | Keep serving until cutover, then 301 → `vehloshare.app`; keep in the Supabase auth allowlist through the transition. |

## Decisions to lock before starting

### Decision 1 — Bundle id
**LOCKED (2026-07-09): change to `app.vehloshare`** (iOS `ios.bundleIdentifier` + Android
`android.package`). Cheap now — no App Store record or EAS build exists yet, so EAS provisions
the new id from the first build with nothing to migrate; and it's the only free window (locked
after first submission). The Associated-Domains appID becomes `TEAMID.app.vehloshare`.
**[YOU]** register the new App ID in the Apple Developer portal with the **Associated Domains**
capability enabled before the first EAS build.

### Decision 2 — App scheme `govehlo://` → `vehloshare://`?
**LOCKED (2026-07-09): yes → `vehloshare://`.** Pre-launch, low risk; installed-app deep links
only, no external dependency.

### Decision 3 — Keep `govehlo.dk` alive during transition?
**LOCKED (2026-07-09): yes.** 301-redirect `govehlo.dk` → `vehloshare.app` and keep it in the
Supabase auth allowlist for a while — confirmation emails/join links already sent point at the
old host. Retire only once nothing references it.

---

## Phase 0 — Availability (before committing)

1. **[YOU] App Store name** — check "VehloShare" is free (see the dedicated section below).
   If free, **reserve it** by creating the app record in App Store Connect (no need to submit).
2. **[YOU] Domain** — register `vehloshare.app` (Cloudflare Registrar is ideal — DNS lands in
   the same place as the Pages project).
3. **[YOU] Trademark sanity** — quick search on EUIPO (EU) and DKPTO (Denmark) for
   "VehloShare" / "Vehlo" in the relevant classes. Not a legal opinion, just a collision check.
4. **[YOU] Socials/handles** — grab the obvious handles so they're not sniped mid-rename.

---

## Phase 1 — Stand up the domain  [YOU]

1. Add `vehloshare.app` to Cloudflare (nameservers / zone).
2. Add `vehloshare.app` (and `www`) as **custom domains on the govehlo-web Pages project**.
3. Decide canonical = `vehloshare.app`. Leave `govehlo.dk` serving for now (redirect comes at
   cutover, Phase 6).
4. If admin is renamed too: plan `admin.vehloshare.app` (mirrors `admin.govehlo.dk`), Cloudflare
   Access rule updated. (Can defer — admin can stay on govehlo.dk longer than the app domain.)

---

## Phase 2 — Email (Sweego + Supabase SMTP)  [YOU]

Mirrors the setup we did for `govehlo.dk`:
1. **[YOU]** Add `vehloshare.app` as a sending domain in **Sweego**. Set the CNAMEs it gives you:
   SPF/bounce (`swg.vehloshare.app`), DKIM (`sweego1._domainkey.vehloshare.app` → the DKIM id
   **for this domain** — don't reuse the govehlo.dk id), and **one** DMARC record on
   `_dmarc.vehloshare.app`.
2. **[YOU]** New sender `no-reply@vehloshare.app`; alias `services@vehloshare.app` for alerts.
3. **[YOU]** Supabase → Auth → Emails (SMTP): change the **sender address** to
   `no-reply@vehloshare.app` once the domain verifies in Sweego. Keep the old sender working
   until then.

> Gotchas we already hit: DKIM CNAME must point at *this* domain's DKIM id (id mismatch = 550
> "Unknown domain"); only **one** DMARC record per name or it's ignored.

---

## Phase 3 — App identity (mobile)  [CLAUDE + YOU]

1. **[CLAUDE]** `app.json`: `name` → "VehloShare", `slug`, `scheme` → `vehloshare` (Decision 2),
   `ios.associatedDomains` → `applinks:vehloshare.app`, `android.intentFilters` host →
   `vehloshare.app`, and `ios.bundleIdentifier` / `android.package` **only if** Decision 1 = change.
2. **[CLAUDE]** Propagate into the **committed native projects** (they're currently stale — the
   GVM-18 app.json config was never prebuilt in): add the Associated Domains entitlement to
   `ios/GoVehlo/*.entitlements`, the intent filter to `AndroidManifest.xml`, and update the
   URL scheme in `Info.plist`. (Or run `expo prebuild` and commit the result.)
3. **[YOU]** Apple Developer portal: if Decision 1 = change bundle id, register the new App ID
   **with the Associated Domains capability enabled**, and update provisioning/signing.

---

## Phase 4 — Universal links (the parked GVM-88 / GVM-23, now on vehloshare.app)

Do this *after* the domain resolves on Pages.
1. **[YOU]** Provide the **Apple Team ID** (10-char, from the Developer account) and — for
   Android, later — the **release signing-key SHA-256 fingerprint** (from the EAS/Play keystore).
2. **[CLAUDE]** Host on `vehloshare.app` via the Pages project:
   - `/.well-known/apple-app-site-association` — JSON, no extension, `appID`
     `TEAMID.<bundleid>`, `paths` `["/join/*", "/auth/*"]`.
   - `/.well-known/assetlinks.json` — package `<bundleid>` + the SHA-256 fingerprint.
   - `_headers`: serve AASA as `application/json`; make sure `_middleware.js` / `_redirects`
     do **not** intercept `/.well-known/*` (must be a plain 200, no redirect).
3. **[CLAUDE]** Switch auth `redirect_to` + join links to `https://vehloshare.app/...`, keeping
   `vehloshare://` as a fallback. Add a lightweight web fallback page for no-app / wrong-device.
4. **[YOU]** Supabase → Auth → URL config: add the `https://vehloshare.app/...` redirect URLs to
   the allowlist (keep the old ones during transition).
5. **Verify:** `curl -I https://vehloshare.app/.well-known/apple-app-site-association` → 200 +
   `content-type: application/json`, no redirect; Apple's AASA validator; then the real
   end-to-end "email link opens the app" test **on TestFlight** (not the simulator — Apple's
   CDN caches AASA; ties into GVM-117).

---

## Phase 5 — Repos, services, and code strings  [CLAUDE + YOU]

1. **[YOU]** Rename GitHub repos `govehlo-web`/`govehlo-mobile`/`fuel_sharing` →
   `vehloshare-*` (GitHub keeps redirects, but update: local remotes, Cloudflare Pages git
   integration, EAS project link, any CI secrets/URLs).
2. **[YOU]** Sentry: rename the `govehlo` org/project (or create new) and update DSNs.
3. **[YOU]** Supabase: rename the project *display name* (ref/URL unchanged).
4. **[YOU]** Better Stack monitors + `/api/health` targets → new domain.
5. **[CLAUDE]** Code sweep across all three repos: user-facing "GoVehlo" → "VehloShare",
   scheme, deep-link hosts, email templates, `manifest.json`, `CLAUDE.md`s, and any hardcoded
   `govehlo.dk` / `govehlo.app` URLs. Jira project *keys* stay `GV`/`GVM`.

---

## Phase 6 — Cutover & cleanup  [YOU]

1. Flip canonical to `vehloshare.app`; add 301 `govehlo.dk` → `vehloshare.app`.
2. Keep `govehlo.dk` in the Supabase auth allowlist + the old email sender alive until nothing
   references them, then retire.
3. Update landing/admin copy, App Store metadata, screenshots.
4. Final end-to-end pass on a TestFlight build: signup email from `@vehloshare.app`, link on
   `vehloshare.app`, opens the app, join-by-link works.

---

## Rollback / safety

- Nothing is destructive if `govehlo.dk` + the old sender stay alive through the transition.
- The Supabase DB never moves, so there is **no data-loss path** in this rename.
- If a step misbehaves, the app keeps working on `govehlo.*` until you cut over.

---

## How to check the App Store name is free

The **authoritative** check is in **App Store Connect** (needs a paid Apple Developer account):

1. Go to **App Store Connect → Apps → the blue "+" → New App**.
2. In the **Name** field, type **VehloShare**. Apple enforces **global uniqueness** on app
   names — if it's taken you get *"The App Name you entered is already being used."*
3. If it's accepted, **creating the app record reserves the name** — you do **not** have to
   submit anything. Do this early so nobody grabs it mid-rename.

Notes:
- The app **Name** (≤30 chars) is independent of the **bundle id** and the **domain** — a free
  name doesn't guarantee a free bundle id, and vice-versa.
- Quick non-authoritative pre-checks: search the App Store app for "VehloShare"; a web search
  (done 2026-07-09 — no obvious car-sharing collision). These don't replace the ASC check.
- Trademark (EUIPO/DKPTO) is a *separate* question from Apple's name uniqueness.

---

## Cutover-day checklist — what's still held for the Apple-gated cutover

The single record of what still has to happen. **Updated 2026-08-06:** everything that did not
actually need the Apple Developer account has shipped, so this is no longer "one coordinated
pass" — it is a short Apple-gated block (0, 2, and the TestFlight tail of 6), two cosmetic
service renames, and the domain 301 which stays LAST. Ticked items carry the date and the PR;
two items **evaporated** rather than completing and are struck through with the reason — they
are kept, not deleted, so nobody re-derives them from the old plan.

### 0. Apple (unblocks the app side)  [YOU]
- [ ] Enroll in the Apple Developer Program. *(GV-320)*
- [ ] Register App ID **`app.vehloshare`** with the **Associated Domains** capability. *(GV-320)*
- [ ] Reserve the App Store name **"VehloShare"** (create the app record; no submit needed). *(GV-320)*

### 1. App identity — mobile  [CLAUDE + YOU]
**Did not need Apple after all** — the whole block landed before enrolment.
- [x] **Done (2026-08-05, mobile #571):** `app.json` — `ios.bundleIdentifier` + `android.package` → **`app.vehloshare`**; `scheme` → the **array** `["vehloshare", "govehlo"]` (`vehloshare` canonical and the only scheme the app emits, `APP_SCHEME` in `src/lib/auth-linking.ts`; `govehlo` kept so pre-rebuild links still open). `ios.associatedDomains` → `applinks:vehloshare.app` and the `android.intentFilters` host were already `vehloshare.app` (GVM-436). **`slug` deliberately stays `govehlo`** — EAS `appVersionSource: "remote"` is keyed to the server-side project record; rename it on expo.dev first, if ever. (`expo.name` was already done, mobile #264.)
- [x] ~~Native projects: rename `ios/` folder + Xcode project + entitlements; AndroidManifest.~~
  **OBSOLETE (2026-08-05) — this step has no target.** `ios/` is gitignored and is regenerated
  by `expo prebuild`; there are no committed native projects to propagate into. The workspace
  and `.app` are named after `expo.name` (**VehloShare**) and the bundle id is compiled in from
  `app.json`, so the first prebuild after #571 produces the new identity by itself.
  `CFBundleDisplayName` → "VehloShare" was already set in app.json's infoPlist (mobile #404,
  2026-07-19).
- [x] **Done (GVM-436):** API base `EXPO_PUBLIC_API_URL` defaults to `https://vehloshare.app` in
  all eight `src/lib/*` call sites. `govehlo.dk` still serves the identical deployment, which is
  exactly why a stale host here goes unnoticed — new call sites default to `vehloshare.app`.
- [x] **Done (2026-08-06, [YOU]):** `vehloshare://auth/callback` added to the Supabase redirect
  allowlist (`govehlo://` kept through the transition). ⚠️ Still true: the scheme is **compiled
  in**, so every existing dev/TestFlight install only answers `govehlo://` until it is rebuilt
  and reinstalled — and the bundle id changed too, so uninstall the old `dk.govehlo.app` once or
  you get two icons.
- [x] **Done (2026-07-19, mobile #404):** `src/lib/notifications.ts` — Android
  notification CHANNEL name flipped `'GoVehlo'` → `'VehloShare'`, done deliberately in a
  reviewed PR (not a sweep-agent find/replace; the file is local-only-guarded per the
  worktree-agent leak lesson) with a regression test pinning the channel name.
- [x] **Done (GVM-518):** support mailto — `src/lib/support.ts` exports
  `support@vehloshare.app`, pinned by a guard test. The old `support@govehlo.dk` is dead, not
  merely legacy.

### 2. Universal links — Phase 4 (after Apple)  [CLAUDE + YOU]
Half-shipped: the **constants** are right, the **Team ID** is not available yet.
- [x] **Done (2026-08-05, web #275):** the identity constants the association files are built
  from — AASA `BUNDLE_ID` and assetlinks `PACKAGE_NAME` → **`app.vehloshare`**, `/join/[code].js`
  `APP_SCHEME` → `vehloshare`, and the announcement composer's `ALLOWED_LINK_SCHEMES` gained
  `vehloshare://` (`govehlo://` kept through the transition).
- [ ] **[YOU]** Apple **Team ID** → the AASA `appID` (`TEAMID.app.vehloshare`). Verify
  `/.well-known/apple-app-site-association` is a plain 200 as `application/json` with no
  redirect (`_headers` serves it; `_middleware` must not intercept `/.well-known/*`), and add
  the Android release signing SHA-256 to `assetlinks.json`. *(GV-320)*
- [ ] Switch auth `redirect_to` + join links to `https://vehloshare.app/...` (keep `vehloshare://` fallback) + a web fallback page.
- [ ] **[YOU]** Supabase redirect allowlist: add `https://vehloshare.app/**`.

### 3. Public web + email flip  [CLAUDE done / YOU]
- [x] **Done (2026-07-09):** web **#87** merged (`3bd8e7e`) — GoVehlo→VehloShare display
  copy (landing, admin, email templates, privatliv, generic push titles) is live. Deliberate early
  flip, not part of the held identity/infra sweep — see the Progress section above.
- [x] **Done (2026-08-06, [YOU]):** Supabase → SMTP sender switched to
  `no-reply@vehloshare.app`, and the rebranded email templates were **re-pasted into Supabase**
  (the repo copies are not live by themselves). This closes Phase 2 step 3.
- [x] ~~Cloudflare Email Routing on `vehloshare.app` (MX + forward rules) for `support@` /
  `services@vehloshare.app`, mirroring govehlo.dk.~~ **OBSOLETE (2026-08-05/06) — superseded by
  Proton Mail.** `vehloshare.app`'s mailboxes moved to Proton in one cutover (MX
  `mail.protonmail.ch` / `mailsec.protonmail.ch`, Proton SPF on the apex, three DKIM CNAMEs) and
  **Cloudflare Email Routing on `vehloshare.app` was retired**. `support@` and `services@` are
  real Proton mailboxes; the Sweego return-path/DKIM rows are untouched, so transactional mail
  still authenticates. DMARC is `p=quarantine` with `rua=mailto:services@vehloshare.app` since
  2026-08-06 — the authoritative record is `docs/dmarc-records.md` in the web repo.

### 4. Domain 301 — LAST  [YOU]
- [ ] **Only after mobile is rebuilt pointing at vehloshare.app:** `301` `govehlo.dk` → `vehloshare.app`. ⚠️ A plain 301 turns the app's `/api/*` **POSTs into GETs (drops the body)** — if any old build might still call govehlo.dk, use **308** for `/api/*` (preserves method+body), 301 for pages.
- [ ] Keep `govehlo.dk` in the Supabase auth allowlist through the transition (Decision 3).

### 5. Repos & services  [YOU]
- [x] **Done (2026-08-06):** GitHub repos renamed `govehlo-web` → **`vehloshare-web`** and
  `govehlo-mobile` → **`vehloshare-mobile`**. GitHub redirects the old names, local remotes were
  updated, and **Cloudflare Pages auto-deploy was verified still firing** after the rename.
  `fuel_sharing` has no "govehlo" in its name — rename was optional and **not taken**.
  ⚠️ The **local working directories keep the old names** (`govehlo-web/`, `govehlo-mobile/`)
  on purpose: every cross-repo tool (`vendor-db-types`, `check-hotpath-mirror`,
  `check-ledger-event-classification`, `check-release-gates`) resolves siblings by
  `../govehlo-*`. Repo name ≠ directory name.
- [ ] Sentry org + Supabase project **display names** → VehloShare (cosmetic; Supabase ref/URL unchanged).

### 6. Store listings — before any submission  [YOU]
- [x] **Done:** `store/apple/*`, `store/google/*`, `store/privacy-policy.html` and
  `store/apple/review-notes.txt` carry **zero** GoVehlo/govehlo strings — verified by grep over
  `store/` in the mobile repo.
- [ ] The end-to-end pass still has to run on a real **TestFlight** build (signup mail from
  `@vehloshare.app`, link on `vehloshare.app` opens the app, join-by-link works) — Apple's CDN
  caches AASA, so the simulator does not prove it. *(GV-320 / GVM-117)*

### 7. Cleanup  [YOU + CLAUDE]
- [ ] Retire `govehlo.dk`'s legacy sender once nothing references it, then harden `govehlo.dk`
  with the parked-domain DMARC record. **Note (verified 2026-08-06):** `govehlo.dk` has **no**
  Sweego rows to retire (`swg.govehlo.dk` and `sweego1._domainkey.govehlo.dk` are both empty)
  and **no DMARC record at all** — the original wording assumed a Sweego setup it never had.
  `docs/dmarc-records.md` in the web repo is the record for what to publish.
- [x] **Done (2026-08-06, GV-230 docs truth pass):** swept the three `CLAUDE.md` files +
  `DEPLOY-CHECKLIST.md` — GitHub repo identities → `vehloshare-*`, local sibling paths left as
  `../govehlo-*` on purpose (with a note saying so), historical mentions kept as history.
- [x] **Done (GVM-518 / GVM-436):** mobile `PRIVACY_URL` and the support mailto are on
  `vehloshare.app`; inbound routing for `support@vehloshare.app` now exists (Proton).
